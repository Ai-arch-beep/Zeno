import express from 'express';import OpenAI from 'openai';const app=express(),ai=new OpenAI({apiKey:process.env.OPENAI_API_KEY});app.use(express.json());app.use(express.static('.'));app.post('/api/chat',async(req,res)=>{try{const r=await ai.responses.create({model:'gpt-4.1-mini',instructions:'You are Zeno, a friendly, concise AI assistant.',input:req.body.message});res.json({reply:r.output_text})}catch(e){res.status(500).json({error:'Zeno could not answer.'})}});app.listen(process.env.PORT||3000);