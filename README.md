# Zeno Web
Tablet-friendly PWA with chat, voice input, and spoken replies.

## Deploy
Use a Node.js hosting service. Set `OPENAI_API_KEY` as a server-side secret/environment variable. Never put the key in `app.js` or `index.html`.
Run `npm install` then `npm start`.
